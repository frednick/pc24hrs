export const metadata = {
  title: 'Next Weekend Football Predictions | Football Predictor',
  description: 'Weekend-only football fixtures and pattern-based predictions using the original supplied club and pick data.',
  robots: { index: true, follow: true },
};

export default function WeekendLayout({ children }) {
  return children;
}
