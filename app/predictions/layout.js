export const metadata = {
  title: 'Football Predictor | Weekend Match Predictions',
  description: 'An independent football prediction dashboard for reviewing weekend match selections, markets, odds and results.',
  keywords: ['football predictions', 'weekend predictions', 'football predictor', 'match predictions', 'football analysis'],
};

export default function PredictionsLayout({ children }) {
  return children;
}
